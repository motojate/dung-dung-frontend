import { Avatar } from '@mui/material';

const LogoIcon = () => {
  return (
    <Avatar
      src="/logo256.png"
      sx={{ width: 64, height: 64, borderRadius: 0 }}
      alt="Logo Icon"
    />
  );
};

export default LogoIcon;
